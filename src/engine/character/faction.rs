use crate::engine::common::ID;

#[derive (Debug)]
pub struct Faction {
    // Some way to store membership
    // Anything else?
}

impl Faction {
    pub fn get_members (&self) -> Vec<ID> {

        todo! ()
    }
}
